# Plain HTTP listener on port 8443 (no TLS).
# Clients attempting a TLS handshake will get a protocol error.
server {
    listen 8443;
    server_name nginx-nocert;

    location / {
        return 200 '{"status":"ok","service":"nginx-nocert"}';
        add_header Content-Type application/json;
    }
}
