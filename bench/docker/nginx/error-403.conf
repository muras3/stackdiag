server {
    listen 443 ssl;
    server_name nginx-403;

    ssl_certificate     /etc/nginx/certs/valid.crt;
    ssl_certificate_key /etc/nginx/certs/valid.key;

    location / {
        return 403 '{"status":"error","code":403}';
        add_header Content-Type application/json;
    }
}
